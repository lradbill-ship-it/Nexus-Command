# NEXUS COMMAND — Living TODO

> **This is the canonical backlog. Keep it updated EVERY task** — check items off when shipped, and add
> new ideas the moment they come up (it will grow as we go).
>
> 🔁 **WORKFLOW RULE (Lane's standing request):** at session start **and after every shipped task**,
> present Lane the **entire current TODO** and let him pick the next task via **multiple-choice cards**
> (the `AskUserQuestion` tool). Keep doing this as the list grows. The tool shows up to 4 cards at once,
> so: put the 3–4 best candidates on cards, and **always also paste the full list (below) in the message**
> so nothing is hidden — Lane can pick a card or name any other item. Then update this file.

Last updated: **2026‑06‑30** (Session 7 in progress — shipped **Legendary Landmarks**, a special‑character‑only map objective). Canonical handoff: **`docs/SESSION_7_HANDOFF.md`**. Session 7 = confirm movement/FPS feel on Lane's machines, then back to features/balance.

---

## ✅ Shipped in Session 7 (newest first)
- **🐛 FIX: combat units freeze + loggers don't deposit under a saturated path budget** (`<deploy>`, live) — **Lane's report** ("no ground unit moves except harvesters; wood harvesters collect but never deposit"). Two latent bugs from this session's pathfinding work: (1) the starvation‑escape forced search had been gated to **SUPPORT units only** (to keep the army off the throttled forced‑search credits) — but that left **combat units + heroes with NO escape**, so under a chronically saturated budget they wait forever. The forced search is already globally throttled (few/tick + small node cap) so it's FPS‑safe for everyone → **removed the support‑only gate**. (2) `followPath` just HELD when a path was budget‑deferred, and states that set a path only ONCE (harvester/logger **return‑to‑deposit**, guard, dig‑approach) never re‑requested it → a deferred delivery path held forever (loggers gathered but never deposited). `followPath` now **re‑requests the path each tick while waiting**, which also triggers the escape. Harvesters kept working throughout because they bypass `findPath` via their **tunnel fallback** — which is exactly why they were the only ground units still moving. Verified under 60‑unit saturation: squad all move, loggers deposit. ⚠ couldn't reproduce Lane's *extreme* saturation locally (my sims don't starve as hard) — fixes target the exact mechanism; **needs Lane's confirm**. tsc + bundle clean, boots clean.
- **🐛 FIX: excavated heroes stuck in a rock‑locked pocket at the vault** (`<deploy>`, live) — **Lane's report** ("heroes come out of the factory and just sit, can't be directed anywhere"). A Hero Vault is buried in a mountain (`T_ROCK`); `freeSpotNear` could unearth the hero (Titan/Devastator/Warden) on a passable tile that is an **isolated pocket** walled in by rock → it jitters a few px but can't path out. `excavate()` now uses **`reachableSpotNear`**: flood‑fills candidate tiles and unearths on the nearest ground in a **large connected region (≥200 tiles)** = the open map. Repro'd: 8 seeds had genuinely trapped heroes (moved ~12px) → after the fix all 8 travel 350–690px. ⚠ this is the EXCAVATED heroes (from Hero Vaults via a Borer/Deep Bore Facility); **special characters from the War Foundry (Jedi/Cartman/…) move fine in every headless test** — if Lane's stuck heroes are those, it's a separate unreproduced issue (asked him to confirm). tsc + bundle clean, boots clean.
- **🐛 FIX BATCH: FPS sink (Envoy), stuck loggers, landmark access, faction naming** (`<deploy>`, live) — **Lane's playtest report.** (1) **FPS dropping hard** → profiled to the **Envoy**: a courting envoy that couldn't reach the settlement CENTRE chased it forever, thrashing `followPath` + stuck‑recovery + 30k‑node re‑paths **every tick** (6 envoys ≈ 26ms/step). Fix: park as soon as within `SETTLE_R` and stop; approach the reachable freeSpot not the centre; throttle re‑path to 0.6s; give up after 20s on an unreachable town + 30s re‑court cooldown (no thrash loop). **Realistic late‑game 26ms → 0.27ms/step** (headless). (2) **Loggers stuck** (crystal/coolant OK) → `nearestForest` had no fallback under a saturated path‑budget (harvesters survive via tunnel); added a throttled forced search → loggers gather under load. (3) **Pathfinding perf hardening**: the starvation‑escape forced search is now **throttled (3/tick) + small node cap**, and `findPath` uses a **generation stamp** instead of clearing the whole 113k‑tile map every call. (4) **Landmarks inaccessible** → heroes stranded on monoliths buried in trees/rock; now placed on passable ground + **road carved to centre** + wider clear → always reachable (headless flood‑fill: all connected to HQ). (5) **Naming**: feed no longer says "VANTA CELL" — uses the real faction name. tsc + bundle clean, artifact boots no errors. **LESSON: profile per‑subsystem before guessing — the FPS sink was NOT pathfinding volume (only ~4 findPaths/step) but one unit type's per‑tick followPath thrash; a unit that can't reach its goal must throttle re‑path + give up, never chase every tick.**
- **🐛 FIX: units permanently stranded by pathfinding‑budget STARVATION** (`<deploy>`, live) — **Lane's 2nd playtest report** ("new harvesters of all types sit outside the war factory, don't go to the rally, don't auto‑harvest, can't be commanded — just stuck"). The earlier re‑path fix wasn't enough: the deeper cause is **budget starvation**. Units are stepped in `game.units` array order; a large active army consumes the entire `resetPathBudget` (60000 pops/tick) *before* later‑processed units are reached, so a fresh unit's `setPath` **always** defers → `waitPath` holds forever. And because `waitPath` short‑circuits `followPath` before stuck‑detection, the harvester's tunnel‑escape never fires either. Re‑pathing every tick doesn't help — every retry re‑defers. Fresh economy units by the factory are hit hardest (appended last in the array). **Fix:** a persistent `waitPath` now escalates to a **FORCED, un‑budgeted `findPath`** after ~1.2s of starvation (`findPath` gained a `force` param that bypasses the budget check; new `Unit.waitSince` tracks the hold). General across ALL unit types — nothing can hang forever. Repro'd headlessly under 70 constantly‑moving units: harvester stuck at factory (`order=move`, `waitPath`, 65px) → now leaves + harvests (`order=idle`, `hState=go`, 218px). tsc + bundle clean, artifact boots no errors. **LESSON: a per‑tick pathfinding budget must have a starvation‑escape valve — array‑order processing means late units can be perpetually starved by an active army; the deferral‑hold from S6 assumed the budget frees within a few ticks, which a big battle violates.**
- **🐛 FIX: harvesters/loggers ignored move clicks under a busy path budget** (`<deploy>`, live) — **Lane's playtest bug** ("when I manufacture harvesters they just won't move once they come out of the factory, no matter where I click"). Root cause: the **harvester & logger move‑order branches in `updateUnit` followed their path but NEVER re‑requested one** — the only two movers the S6 "every move order must re‑path" fix missed. When the per‑tick pathfinding budget is **saturated** (busy mid‑game, many units/economy scans), a manual move's `setPath` defers to `waitPath` with a null/stale path, so the unit holds forever or trails its **old harvest path** instead of going where you clicked. Fix: both branches now re‑path on `waitPath` / consumed‑or‑empty path / a 0.8s timer (matching the combat/Aegis/Envoy movers) and clear `waitPath` on arrival. Repro'd headlessly via a **0‑budget tick** at click time → harvester ended 690px from a 320px‑away target (stuck) → now reaches it (51px, order idle). tsc + bundle clean, artifact boots no errors. **LESSON (extends the S6 path rule): EVERY move branch — incl. the economy units' own dispatch — must re‑path; a budget‑deferred `waitPath` with no retry = a permanently stuck unit.**
- **🗿 Legendary Landmarks — a special‑character‑only map objective** (`dcb6ced`, live) — Lane's ask: a skirmish accomplishment that *requires* special characters. **3 ancient monoliths** spawn each match (mid‑ring, always visible). **Only a `unique` special character can channel a claim** (ordinary units can't — `landmarkTick` tallies only `U[u.type].unique` units within `LANDMARK_R`=3 tiles, ~5s channel). Holding one **trickles crystals + alloy** AND radiates a **bonus themed to the character that claimed it** (one shared mechanic, slice of the "character quest" idea): **Cartman→Cartmanland** (×2 income + happiness), **Bounty Hunter→Bounty Cache** (×2 windfall + alloy stream), **Kenny/Kyle** (heal aura), **Stan/Jedi/Droideka** (+dmg/+speed buff aura), **Sith→Dark Nexus** (sears nearby enemies). Attune persists while owned (the character can leave to fight). **Contestable hero‑vs‑hero** — an enemy character wrests it back (no defender present). **AI parity** (`aiLandmark`: idle special characters seek & claim the nearest landmark). Right‑click a monolith with a special character selected → send them to hold it. New `Landmark` type + `game.landmarks` + render (monolith + halo + capture ring + aura footprint + minimap marker) + help/intro. **7/7 headless** (ordinary‑can't‑claim, jedi claims + windfall, Sanctuary heals, Dark Nexus drains, enemy reclaims, AI claims); tsc + bundle clean, artifact boots no errors. ⚠ the monolith *look* + feel is RAF‑gated headless → Lane's eyes on device. **Knobs:** `LANDMARK_R` 3·TILE, `LANDMARK_CLAIM` 5s, `LANDMARK_INCOME` 4/s, `LANDMARK_ALLOY` 0.5/s, `LANDMARK_WINDFALL` 600, `LANDMARK_FX_R` 5·TILE, `LANDMARK_HEAL` 16, `LANDMARK_DRAIN` 14; 3 landmarks/map (mapgen). **Pattern reused:** relay/settlement capture loop + auraTick(heal)/buffUntil(buff)/damage(drain).

---

## ✅ Shipped in Session 6 (newest first)
- **⚡ PERF & PATHFINDING RESCUE (playtest-driven, 9 fixes, all live)** — Lane hit severe FPS + movement problems on his 2019 MacBook Pro; each root-caused + fixed via headless step-timing / behavior repros:
  1. **Slow-motion below 20fps** (`123bc25`) — the loop stepped the sim by one frame-dt clamped to 0.05s, so under 20fps the sim advanced *less* than real time → half-speed. Replaced with a **fixed-timestep accumulator** (catches up in 1/60 steps; `MAX_CATCHUP` 12, `BUDGET_MS` 10 wall-clock guard). 5/5 (10fps tracks real time vs old half-speed).
  2. **109ms harvester pathfind** (`bafc20c`) — the A* binary heap swapped via array-destructuring (`[a,b]=[b,a]`) → an alloc per sift → GC stall. **Allocation-free heap: 109ms→14ms** (7.7×).
  3. **Assault-time repath storm** (`3907d53`) — many units pathing in one tick stacked into an 83ms step → per-step path budget. (later reworked, see #7).
  4. **Big-battle particle render** (`ede67e0`) — explosion/ring/flash pushes bypassed the 520 cap; **hard-capped to 300 (90 in Low Detail)** in the per-step particle update (`setLowDetail`/`partCap`). Verified under a dense brawl.
  5. **FPS counter + Low Detail (F) + adaptive quality** (`abc891c`/`5933196`) — top-left FPS readout; **F** drops water/dust/vignette + particles; **auto-enables Low Detail when FPS <40 sustained, restores >56** (manual F overrides; `adaptQuality`). Mac Mini 45–75fps.
  6. **Loggers idle / economy scans starved** (`5a0b21d`) — the per-step *call-count* budget (6) starved multi-call scans (`nearestForest` fires up to 48 findPaths). **Re-budgeted by A* WORK (node pops), not calls.** Loggers auto-collect again.
  7. **Units wedged in terrain** (`5a0b21d`+`2569a14`) — restored node budget + **always-on anti-wedge** (nudge any ground unit off an impassable tile every tick, even idle — covers Borer-mined heroes spawned in rock) + **clearance-aware dislodge that escalates to a hard-escape** (`nearestOpenTile`; after ~3s stuck, teleport to the nearest roomy tile toward the goal).
  8. **Units "hung up all over the place"** (`617a5e1`) — THE big one: the **move/amove handler never re-pathed** → a unit followed one path and stalled if it didn't reach the goal. Now move/amove (+ transport/Aegis/Envoy) **repath every ~0.8s & on path-consume**, findPath returns a **PARTIAL path** to its closest node when the goal is past budget (incremental long hauls), and budget-deferred searches **HOLD + retry** (`waitPath`) instead of wedging. Headless: 24/24 units cross real terrain mid-range (was 0/24).
  9. (perf-budget tuning rolled into the above: `resetPathBudget` 60000 pops/tick, node cap 30000, `pathDeferred()`.)
  ⚠ **Lane's 2019 laptop FPS + overall movement feel still to be re-confirmed** (Mac Mini good; movement verified headlessly).
- **Droideka — Star Wars special character: deployable personal deflector shield** (`0a59e71`, live) — `U.droideka` (Cyber-gated, unique, 950cr): a rolling destroyer droid. New mechanic (`UnitDef.selfShield` + `Unit.shieldE`): while **deployed (stationary)** it soaks incoming damage 1:1 from a 750 pool (`applySelfShield` in `damage()`), recharging when out of fire; the shield is **DOWN while rolling** (vulnerable in transit). Twin rapid blasters. AI parity. Tripod sprite + icon + deflector-dome overlay. 6/6 headless (absorbs all, drains 1:1, overflow→HP, depletes, down-while-moving, recharges 100→380). **Knobs:** `selfShield` 750, `SELF_SHIELD_REGEN` 70, `SELF_SHIELD_DELAY` 2.5.
- **Bounty Hunter — Star Wars special character: jetpack + homing seeker salvo** (`97ef6ba`, live) — `U.bountyhunter` (Cyber-gated, unique, 950cr): a fast **airborne** merc (flies over terrain, only AA hits her) that every ~6s locks a **salvo of up to 4 homing seeker missiles** onto distinct nearby enemies (new `UnitDef.seekerSalvo` + `seekerSalvoTick`, slow splash projectiles via the homing-shot system). New mechanic = multi-target homing salvo on a flying character. AI parity. Jetpack/T-visor sprite + icon. 3/3 headless (airborne, 4-target salvo, seekers home + hit 4/6). **Knobs:** `SEEKER_CD` 6, `SEEKER_RANGE` 300, `SEEKER_DMG` 56, `SEEKER_SPLASH` 34, `SEEKER_SPEED` 300, `seekerSalvo` 4.
- **Sith Lord — Star Wars special character: Force Lightning** (`b530e67`, live) — `U.sith` (Cyber-gated, unique, 1000cr): every ~9s casts **Force Lightning** (new `UnitDef.forceLightning`) — a chain that arcs through up to 4 nearby enemies dealing falloff damage **+ a 2s stun** each (`forceLightningTick`, reuses the `'arc'` bolt FX). Distinct from Cartman's instant AoE stun (no dmg/chain) and the Arc Tower's pure-damage chain (no stun). AI parity. Black-robe/violet-lightning sprite + icon. 3/3 headless (casts, chains+damages 4/6, stuns 4/6). **Knobs:** `FORCE_CD` 9, `FORCE_RANGE` 190, `FORCE_JUMP` 135, `FORCE_DUR` 2, `FORCE_DMG` 70, `forceLightning` 4.
- **Jedi Knight — Star Wars special character: blaster-deflect** (`0c17c90`, live) — `U.jedi` (Cyber-gated, unique, 900cr): a fast saber duelist that **DEFLECTS incoming direct fire** (new `UnitDef.deflect` 0.5, checked in `updateShots`) and **reflects the bolt back** at the firer for 60% damage; hard-countered by splash/AoE (only direct shots deflect). AI parity. Hooded-robe + lightsaber sprite + icon. 5/5 headless (deflects ~45% of 300, control takes all, every deflect spawns a reflect bolt at the shooter). **This kicks off a fresh Star-Wars-themed special-character line — the South Park boys stay as-is.** **Knobs:** `U.jedi.deflect` 0.5, reflect frac 0.6.
- **Arc Tower — chain-lightning anti-swarm building** (`affb52f`, live) — new `B.tesla` (650cr + 180 alloy, power −25, needs alloy): a point-defense whose zap **arcs between up to 4 clustered ground enemies**, damage falling off per hop, so a single Arc Tower shreds massed infantry/militia where the single-target Sentinel Turret can't. Range 200 / dmg 22 / rof 1.5. `teslaZap()` hooked into the turret branch of `updateBuilding` (fires instantly, no barrel traverse); new `'arc'` particle (jagged double-stroke bolt, `Particle.x2/y2`); Tesla-coil building sprite + sidebar button/icon + DEFENSE legend entry. **AI parity** (`aiTech` builds up to 2, alloy-gated, t>320). 5/5 headless (chains to 4, reaches beyond its own range via the chain, caps at 4, isolated enemy spared, damage falls off −44→−16); tsc + single-file build clean, artifact boots no errors. ⚠ the lightning *look* is RAF-gated headless → Lane's eyes on device. **Knobs:** `TESLA_JUMP` 120, `TESLA_FALLOFF` 0.72, `chain` 4, `B.tesla` dmg/range/rof/cost.

---

## ✅ Shipped in Session 5 (newest first)
- **Polish: Conquest victory/defeat screens** (`faccb37`, live) — celebratory 🏆 victory (glowing title + territory/War-Tech summary) and somber 🏴 defeat, centered, reusing the verified panelBox styling. Victory screen driven-to + screenshot-verified.
- **Polish: UI chrome** (`7e9800c`, live) — intro title glow + glowing underline accent; tactile primary buttons (hover lift + gold glow + active press); faction-card + command-button hovers. Screenshot-verified.
- **Polish: weightier death explosions** (`c6d249f`, live) — crisper death pop (initial spark burst + brighter/longer core flash + hot white inner flash + a touch more fire/ember/debris). Sim-side; clean build. (Look = device call.)
- **Polish: combat hit-flash + punchier impacts** (`d6ce36e`, live) — units & buildings flash hot-red ~0.08s when damaged (`Entity.hitT` set in `damage()`; tint in syncUnit/syncBuilding, composes with Overcharge/construction tints); bumped impact spark + a crisp flash particle per direct hit. tsc/bundle clean, boots clean. (Feel = device call.)
- **PERF: killed the GC stutter spikes (measured)** (`f606435`, live) — headless step-timing review (all-war loads to 554 units) found the sim is fast (avg <2ms) but had ~30ms spikes that were **count-independent → GC pauses**. Fixed two per-frame allocators: `rebuildUnitGrid` reallocated ~15k cell arrays ×2/frame → now allocated once + reused (clear via `length=0`, only used cells); `separation` allocated a closure per unit/frame → now inline 3×3 cell scan (zero alloc). Result: avg ~0.6–1.9ms, **p95 collapsed** (medium 20.9→1.0ms, heavy 4.4→1.8ms). Residual ~24ms major-GC ~1%/frame remains (fixed cost; acceptable). `computeVision` confirmed non-issue (0.1ms). Behavior unchanged. ⚠ Real-device heavy-load FPS still Lane's call (render side is RAF-gated headless); sim is no longer a bottleneck.
- **Polish: Conquest world-map glow-up** (`7b15a53`, live) — gradient backdrop + inset shadow, glowing node halos (SVG blur), glossy territories, animated pulse on a threatened territory, highlighted player-frontier lines, hover states, outlined labels. (Verified via screenshot.)
- **Conquest: War-Tech carry-over upgrades** (`9a4b6f0`, live) — winning a battle (capture or successful defense) advances persistent War-Tech (0→6), each level = +8% army HP & damage for every subsequent campaign battle (a real snowball beyond the credit bonus). Persists to localStorage; shown on the map. sim: `campaignDmgMul/HpMul` + `setCampaignBuffs()` (player-only; HP at `addUnit`, dmg in `fireAt`; reset by `resetSimLocals` so skirmish is unaffected); `main.ts` re-applies after `newMatch`. 6/6 headless + artifact-verified. **Knobs:** `TECH_MAX` 6, `TECH_STEP` 0.08.
- **South Park boys — Kenny / Stan / Kyle special characters** 🎿 (`409ea25`, live) — three more one-per-faction Cyber-gated characters with signature mechanics, all AI-parity'd: **Kenny** (fragile but IMMORTAL — respawns at HQ 18s after death; `respawnQueue`/`respawnTick`, `UnitDef.respawns`, unique cap honors a pending respawn), **Stan** (leadership RALLY aura — allied combat units within 150px get a continuous +dmg/+speed buff via `rallyTick`/`UnitDef.rallyAura`, reuses Overcharge's `buffUntil`), **Kyle** (medic heal aura, reuses `auraHeal`). Sprites + icons + help. 8/8 headless + artifact-verified. **Knobs:** `U.kenny.respawns` 18, `U.stan.rallyAura` 150, `U.kyle.auraHeal` 14. Pattern for a new character = `U.x` (+ unique/hero + a signature flag) + behavior tick + sprite + sidebar + AI special-pick list.
- **Cartman — special one-per-faction character unit** 🎿 (`0601918`, live) — Lane's request. `U.cartman` (Cyber-gated, 900cr, hero/unique, 660hp, slow, tanky, armed): every ~13s bellows "RESPECT MY AUTHORITAH!" → stuns nearby enemies 3s (`authoritahTick`). One per faction (new `UnitDef.unique` enforced in `trainUnit` + AI). Rotund sprite (red coat / teal beanie / yellow puff) + icon + help line. AI parity (cyber-owning AI may field one). New flags `UnitDef.unique`/`authoritah`, `Unit.authT`. 5/5 headless + artifact-verified. **Knobs:** `AUTHORITAH_CD` 13, `AUTHORITAH_DUR` 3, radius `U.cartman.authoritah` 130.
- **Conquest: real stakes — AI incursions + defense battles (slice 2)** (`bc5717c`, live) — the world map is now a two-way war. Each world turn, an AI bordering a player territory may INVADE it → forces a DEFENSE battle (threatened territory pulses red + ⚔ DEFEND button; offense blocked until resolved). Lose → the territory flips to the attacker (the campaign is now losable); win → held. `battleMode`/attacker tracked, persists mid-incursion. 9/9 headless + artifact-verified. **Still open:** defender-tailored battle setups, carry-over upgrades/veterancy, mission-objective variants.
- **EPIC: Conquest Campaign — Risk-style metagame (slice 1)** (`5a1a37b`, live) — a whole new mode beside skirmish. 'CONQUEST CAMPAIGN' on the intro opens a DOM/SVG world of 12 territories (player + 5 AI homes + neutrals, adjacency graph). Select an owned ★ territory → click a bordering enemy/neutral → launches the normal skirmish for it; win captures it, loss holds. AIs take a world turn after each battle (expand into neutrals). Carry-over: +600cr reinforcement per territory held. Win = own all 12; persists to localStorage (resume on reload). New `src/conquest.ts` (self-contained); `main.ts` routes battle-end to the campaign. DOM-based → renders reliably. 13/13 headless + artifact-verified. **Follow-on slices:** tailor each battle to the defending faction (vs the generic 6-way); AIs attack PLAYER territories (real risk of losing ground); carry-over unit veterancy/upgrades, not just credits; map variety / bigger maps; mission-objective variants per territory.
- **Shield Projector — damage-absorbing field building** (`2b1e1a8`, live) — new defensive structure (`B.shieldgen`, 950cr+200 alloy, needs power): projects a field over allies within 6 tiles that absorbs 50% of incoming damage from a recharging reserve (1200 pool, +45/s scaled by power). Defensive counterpart to Overcharge/Iron Dome. Hooked in `damage()` via `applyShield()`; doesn't shield itself. AI parity (`aiTech` builds one, alloy-gated). Field-dome render + absorb shimmer. 5/5 headless (halves ally damage, reserve consumed, enemies unprotected, recharges, AI builds it). **Knobs:** `SHIELD_R` 6·TILE, `SHIELD_ABSORB` 0.5, `SHIELD_MAX` 1200, `SHIELD_REGEN` 45.
- **Combat depth ×4 — Chrono Freeze, Carpet Bomb, APC Transport, Sentry Pod** (`3322746` + `df2c7be`, live) — Lane's "all of it" combat-toy batch. **Chrono Freeze** (Cyber, key Z, 1500cr/130s): freezes every enemy unit in a 200px zone for 6s (can't move/fire). **Carpet Bomb** (Silo, key X, 2600cr+200 alloy/170s): a bomber drops a LINE of 7 blasts across the target, oriented along the run from your HQ — uninterceptable, wide area denial. **APC Transport** (▣500, capacity 5): fast armored unarmed carrier — right-click a friendly infantry to load, press U to unload; spills survivors out wounded if destroyed. **Sentry Pod** (▣360): mobile pod ⇄ fixed turret — drive forward + Y to deploy into a turret, select the turret + Y to pack it back up. **AI parity on all four** (casts chrono at clusters, picks carpet from the silo pool, deploys pods, ferries infantry by transport). 10/10 headless; tsc + bundle clean, artifact boots no errors. ⚠ FEEL/balance want Lane's playtest. **Knobs:** `CHRONO_R` 200, `CHRONO_DUR` 6; `CARPET_COUNT` 7, `CARPET_SPACING` 78, `CARPET_R` 112; transport `capacity` 5; pod deploys to a `turret` (B.turret stats).
- **Civilian diplomacy — Envoy + settlement affinity + town development** (`d0df3b4`, live) — the full "Hearts & Minds" arc (design‑first pick). **Envoy** (`U.envoy`, new unarmed diplomat support unit): right‑click a **neutral settlement** to *court* it — goodwill (`affinity`) builds ~15s → the town **voluntarily joins** (no resentment, +12 happy, a development head‑start). A lone envoy no longer triggers the 6s presence‑flip — pure diplomats win towns via affinity, troops/workers still flip by presence. **Development:** own a town → invest crystals over time (`DEV_COST`/s) to raise it through **3 tiers** — each scales the crystal yield (×1.6/tier), unlocks a **coolant** trickle (tier 2) then **alloy** (tier 3), grows pop toward `DEV_POP_CAP` 60, and at tier 3 raises a **local infantry guard**; station an Envoy to develop ~3× faster. **AI parity:** AI fields Envoys and dispatches them to court neutral towns. Help/legend + sprite + sidebar icon added. **12/12 headless** (courtship timing, no spontaneous join, dev tiers, envoy‑speeds‑dev, unarmed‑no‑crash, AI parity); tsc + bundle clean, artifact boots no errors. ⚠ FEEL/balance want Lane's playtest. **Knobs:** `AFFINITY_JOIN` 100, `AFFINITY_COURT` 6.5, `AFFINITY_DECAY` 2.0, `DEV_MAX` 3, `DEV_BASE` 0.05, `DEV_ENVOY` 0.12, `DEV_COST` 1.1, `DEV_POP_CAP` 60.

---

## ✅ Shipped in Session 4 (newest first)
- **FIX: perf storm — stuck garrison units thrashing A*** (`f692352`, live) — **playtest slowdown** (Lane). The AI garrison order pathed infantry to a building's **solid centre** → A* failed → units stuck in `'enter'` re‑running a full failing pathfind every 0.5s forever, compounding all match. Fix: AI garrison now paths to a passable spot **beside** the building (like the player); a **15s give‑up timeout** on the `'enter'` order so any unreachable garrison aborts to idle instead of thrashing; plus a **240 global mine cap** (AI lays minefields all match). 4/4 headless. **Also restored nexus‑dev/nexus‑dist to the root `.claude/launch.json`** (preview tool reads the project‑root one, which had lost them → was launching the trading server).
- **FIX: settlement capture‑windfall farming exploit** (`3f2496a`, live) — **first playtest bug** (Lane). A contested settlement ping‑ponged and each flip re‑granted loot + recruits + duplicated pop. Fixed: pop **transfers** (prev owner loses it), windfall (loot+recruits) only when taking from **NEUTRAL** (not faction‑to‑faction), and the **owner's presence defends** it (must clear defenders to flip — no rapid back‑and‑forth). 10/10 headless.
- **Garrison mechanic** (`132ddc6`, live) — right‑click a friendly building with **infantry** → they board it (cap 5); the building lays **defensive fire** (~160px, every occupant adds a barrel) and shelters them until it falls (then they spill out **wounded**); press **U** to unload. New `'enter'` order (paths to a spot beside the solid centre), `Building.garrison`, fire in `updateBuilding`, eject in `destroy`. Selection panel shows occupancy. AI parity (tucks spare idle infantry into buildings). 7/7 headless. **Knobs:** `GARRISON_CAP` 5, `GARRISON_RANGE` 160, `GARRISON_ROF` 0.7.
- **Deploy Minefield ability** (`254a026`, live) — Cyber‑gated (key **L**, 700cr, 90s cd): scatter 8 **hidden proximity mines** that arm in 1.5s and detonate when an enemy wanders within 30px (anti‑personnel blast; allies safe). New `Mine` entity (`game.mines`)+`mineTick`; own mines show as blinking markers, enemy mines hidden. **AI parity** (AI mines its HQ approach, `AIState.mineT`). 9/9 headless. **Knobs:** `MINE_COUNT` 8, `MINE_SPREAD` 115, `MINE_ARM` 1.5, `MINE_TRIGGER` 30, `MINE_R` 92, dmg 240/120.
- **UX polish — production + cooldown feedback** (`9e6cbd0`, live) — unit buttons show a **queue‑count badge (×N)** + a **green build‑progress bar**; ability/covert buttons show a **cooldown countdown ('12s')** over the icon (not just the thin bar). Wires the unused `.qbadge` + new `.cdtxt`. (Pre‑existing: disabled dimming, hotkey hints already there.)
- **Overcharge — combat‑stim ability** (`1f11918`, live) — first *friendly buff* ability (Cyber, key **V**, 600cr, 75s cd): your combat units in a zone hit **1.4× harder & move 1.35× faster for 9s**. Support excluded; warm tint marks buffed units. **AI parity** (cyber AI stims its army before a push, `AIState.buffT`). 8/8 headless (incl. measured +1.35× speed, +dmg, 9s expiry). **Knobs:** `OVERCHARGE_R` 150, `OVERCHARGE_DUR` 9, `OVERCHARGE_DMG` 1.4, `OVERCHARGE_SPD` 1.35.
- **Spectre — cloaked stealth unit** (`c8b2162`, live) — new Cyber‑gated strike skimmer (520cr+120 alloy): **invisible & untargetable until it fires or an enemy gets within 3 tiles**. Hit‑and‑run raider; fragile (120hp). Counterplay = proximity reveal + reveal‑on‑fire (spread out to spot enemy ones). **AI parity** (cyber‑owning AI fields them). 6/6 headless (cloak gating, proximity + fire reveal). Render: enemy cloaked hidden, own cloaked shimmer (RAF‑gated → Lane's eye). **Knobs:** `DETECT_R` 3·TILE, `REVEAL_LINGER` 1.6, stats in `U.spectre`.
- **Orbital Ion Strike superweapon** (`a64ada6`, live) — new Silo‑gated ability (key **O**, 3500cr+400 alloy, 200s cd): near‑instant ion beam from orbit, short telegraph, **small 95px blast, UNINTERCEPTABLE** (no missile to dome) — surgical building‑killer + a counter to Iron‑Dome turtling, distinct from the nuke's wide AoE. **AI parity** (silo‑owning AI picks it too, distinct INBOUND warning). 6/6 headless (damages through an enemy dome; control nuke intercepted). **Knobs:** `ORBITAL_R` 95, `ORBITAL_TRAVEL` 1.6, detonate dmg 1500/2400, cost/cd in `ABILITIES.orbital`.
- **Click feed alerts → jump to map** (`9eb50ec`, live) — located events (inbound missiles, militia uprisings, the Free Legion rising, relay grabs/losses) carry a world position; those feed rows show a **⌖** + hover highlight and are **clickable → camera centers there** (with a ring ping). Desktop click + mobile tap. Plumbing: `logMsg` gains optional `at` (state→sidebar) + `focusCamera`/`setFocusHook` bridge to the Phaser camera. 4/4 headless (DOM half; scene‑side camera is RAF‑gated → Lane's device).
- **Repair Rigs mend buildings — explicit order** (`d934945`, live) — they always *could* (idle auto‑search includes buildings) but you couldn't assign one to a SPECIFIC building (escort was unit‑only) → looked broken. Now **right‑click a damaged building with a Repair Rig → it mends THAT building**. Widened `Unit.guard` to `Unit|Building`. **Reminder: rigs burn WOOD — with none they can't repair anything** (the usual confusion); help panel now says so. 5/5 headless.
- **Explosion audio variety** (`2af2e56`, live) — layers a randomized sub‑thump + occasional debris crackle under the real CC0 explosion sample so repeated blasts vary in weight/character. **No new assets** (+172 bytes, code only); `playSample` already pitch‑varies. Weapon/UI fire stays procedural by design (too fast for samples). ⚠ sound is Lane's ear; real CC0 weapon/UI sample packs still a by‑ear curation task (pipeline proven).
- **Animated water shimmer** (`713db02`, live) — a slow diagonal light sweep + finer cross‑ripple that glints on wave crests across visible water (new additive `waterGfx` layer, depth −90, under fog). Viewport‑culled + crest‑thresholded so only a few on‑screen tiles draw; skips dried tiles; ~30Hz. tsc + bundle clean, no console errors. ⚠ the *motion/look* can't be seen headlessly (Phaser RAF pauses when backgrounded) → Lane's eye on device. **Knobs:** thresholds 0.5 / `*0.22` alpha / sweep speed `t*1.1`+`t*1.7` in `drawWater()`.
- **Explicit Command Relay targeting** (`a4120bd`, live) — right‑click an enemy/neutral **Command Relay** with troops selected → the selected military `amove` onto it in formation to **seize & hold** it (neutral falls to presence; an enemy's gets shot offline en route). Support units excluded. Previously relays were only auto‑assaulted by whatever military happened to be present. Headless‑verified 4/4; intro/help updated.
- **Sue for peace / ceasefires** (`eb2ffc0`, live) — wars can now END diplomatically, not only by annihilation, so the emergent **Free Legion is negotiable**. Player **SUE FOR PEACE** button in the Diplomacy panel (enabled at war) pays 400cr reparations; the target weighs the bid by how the war's going for it + persona (warlords resist unless beaten, merchants keen). **Proactive AI peace:** a badly‑losing faction de‑escalates — AI↔AI auto‑signs a ceasefire; an AI losing to the player stands up a **free standing offer** (button → ACCEPT PEACE) but never auto‑ends the *player's* war (annihilation stays the player's call). New `dip.truce` (pair→expiry) pins relations at the truce floor (−10) for 90s so peace doesn't instantly drift back to war; declaring war clears it. Headless‑verified 11/11; intro/help document it. **Tuning knobs:** `TRUCE_TIME` 90, `PEACE_COST` 400, `PEACE_PROPOSE_CD` 25, `TRUCE_FLOOR` −10, `PEACE_THRESH` per persona (warlord 0.35 … merchant 0.62).

---

## ▶ Up next — top candidates (curate to 3–5; these go on the cards first)
- [ ] **🎯 CONFIRM MOVEMENT + FPS FEEL on Lane's machines — the #1 Session‑7 move.** All of S6's pathing/perf rescue is headless‑verified (24/24 cross mid‑range terrain; sim ~2ms; Mac Mini 45–75fps) but needs Lane's real‑play read: do units (esp. heroes/specials/groups) move without hanging across rock/trees/long hauls, and is the **2019 laptop** FPS acceptable now? If a unit still hangs → screenshot + heading; likely a **river/no‑bridge local‑minimum** (fix = a periodic full‑budget search when a unit is barrier‑stuck).
- [ ] **🎮 PLAYTEST‑DRIVEN BALANCE + FEEL PASS.** The toolbox is huge & mostly unplaytested — the **5 new S6 toys** (Arc Tower / Jedi deflect / Sith Force‑Lightning / Bounty Hunter salvo / Droideka shield strength + cost), the South Park chars, **Conquest pacing** (incursion freq, War‑Tech snowball), **Shield Projector**, **Chrono/Carpet** cost, civilian‑diplomacy timing. Ask what felt off → turn the §8 knobs. **Ask‑first** per `CLAUDE.md`.
- [ ] **Conquest depth (slices)** — defender‑tailored battles (battle reflects the defending faction + territory strength); carry‑over unit veterancy (beyond credits/War‑Tech); mission‑objective variants per territory; bigger/varied world maps.
- [~] **More combat depth / characters (additive)** — ✅ **Arc/Tesla tower** (S6, `affb52f`); ✅ **Star Wars characters: Jedi (deflect) + Sith (Force Lightning)** (S6). **NEW DIRECTION (Lane, S6): special characters are now STAR WARS–themed with FRESH mechanics; South Park boys stay as-is.** ✅ **Bounty Hunter** (jetpack + seeker salvo), ✅ **Droideka** (deployable personal shield) — both S6. Open SW ideas: **Wookiee** brawler (tanky melee + stun-immune rage), **Astromech droid** (mobile repair + buff support), **Force-pull/throw** ability, **AT-ST/walker** heavy, a character **team-up synergy** buff. Still open non-char: repair‑reload, cloak‑field, EMP‑grenade; character team‑up synergy buff.
- [ ] **Battlefield visuals (needs Lane's eyes)** — richer sprites/lighting/FX. Render is RAF‑gated headless → can't verify; have Lane direct it from his device. (Verifiable UI polish is largely done.)
- [ ] **More CC0 audio (optional, by‑ear)** — real CC0 weapon/UI samples + distinct explosion variants. `src/assets/audio/` (OGG→AAC via `afconvert`).
- [ ] **Deeper perf — Web Worker sim** — 🛑 **HELD.** Session‑5 perf pass made the sim very fast (avg <2ms/step, GC spikes killed). Unwarranted unless Lane's device shows a render‑side FPS problem specifically.

## Backlog — Features
- [ ] **Emergent factions** — neutral civilian populations coalesce into a NEW faction that can grow into a real threat (note #3).
- [ ] **Richer settlement absorption / civilian diplomacy** — recruit/adopt populations; absorbing faction gains their infrastructure + resources (note #7).
- [x] **Explicit per‑unit relay targeting** — ✅ Session 4 (`a4120bd`): right‑click a relay with troops to send them to seize & hold it.
- [~] **Conquest Campaign** — ✅ slice 1 shipped (S5, `5a1a37b`): world map + invade-to-capture + carry-over credits. **Next slices** (see Shipped entry): defender-tailored battles, AIs attack player territory, carry-over upgrades/veterancy, mission-objective variants.
- [ ] **Scripted mission mode** — authored scenarios with briefings/objectives (distinct from the emergent Conquest map); long-deferred separate mode.
- [~] More superweapons / abilities — ✅ **orbital strike** (`a64ada6`), ✅ **chrono‑freeze** + **carpet‑bomb** (S5); still open: shield‑generator, EMP‑grenade, etc.
- [~] Combat‑depth — ✅ **stealth (Spectre)** `c8b2162`, ✅ **minefield** `254a026`, ✅ **garrison** `132ddc6`, ✅ **transport/APC + deployable Sentry Pod** (S5), ✅ **chrono‑freeze + carpet‑bomb** (S5); still open: repair‑reload, shield‑generator.

## Backlog — Art / Audio / Polish
- [ ] **Real CC0 explosion/smoke sprite sheets** on deaths (deferred; harder to verify headlessly than terrain).
- [ ] **Richer unit/building sprites** — procedural upgrade, or CC0 where it maps cleanly (units are small at game zoom → lower verifiability).
- [x] **Water shimmer / texture pass** — ✅ Session 4 (`713db02`): animated additive sheen (sweep + ripple) on visible water. Could still add a CC0 water *texture* on the static base.
- [ ] UI polish / animations; a tech‑tree or help panel; on‑screen hotkey hints.

## Backlog — Balance & AI
- [ ] **AI parity pass** (also up top) — AI builds domes/silos/wood/repair/water/borer/heroes.
- [ ] **Tuning pass after playtests** — many Session‑2 features are compile‑only; balance once Lane plays.
- [ ] Veterancy tuning (kill thresholds 2/5; dmg/HP factors 1.2 / 1.5; elite self‑repair 4 hp/s).
- [ ] Game‑speed control / pause.

## ⚠ Needs Lane's playtest confirmation (shipped but compile‑verified only)
- [ ] **Missile system feel** — thermonuke base‑wipe, Iron Dome + Aegis interception, AI missile cadence, inbound reticles.
- [ ] **Veterancy** — promotions fire, chevrons render, bonuses feel right.
- [ ] **Hero loop** — Survey Hunter auto‑hunt finds vaults, Borer excavates, the 3 heroes behave (Warden aura heals, Devastator out‑ranges, Titan tanks).
- [ ] **Water harvesting** + dried tiles; coolant abundance.
- [ ] **Turrets/flak** — new range; turrets engaging ground war‑enemies (logic reviewed = sound).
- [ ] **Tree/terrain art** on a real device; late‑game framerate on the 3× maps.

---

## ✅ Shipped in Session 3 (newest first)
- **Coolant economy fix — activity-scaled consumption** (Lane: 50+ tankers still peaked at −64 coolant/s with Wraiths stationed around the map). Units now burn **full coolant only while active (moving or recently fired); idle/stationed units sip ¼** (`COOLANT_IDLE=0.25` in `waterOf`). So a standing defensive air force is cheap to keep and coolant becomes a cost of *active* combat & maneuver. Verified: 10 idle Wraiths = 15/s vs 60/s active. (Pairs with collector-merge to crush the tanker spam.)
- **Patrol / area-guard order** (Lane: guard a whole area, auto-engage enemies beyond limited weapon range). Select combat units → **P** (or tap **⚑ PATROL AREA**) → click a spot: they hold the post and **hunt any enemy within a 12-tile zone** (far past weapon range), chasing within the zone and returning when it's clear. New `'patrol'` order + handler, `armPatrol`/`patrolOrder`/`canPatrol`, a green coverage ring at the cursor while arming, mobile button, help entry. Sentry-style: senses intruders in-zone (ignores fog). Verified: a patrol strike engaged an enemy 320px away (124 weapon range); a plain idle unit ignores the same enemy.
- **Combine collectors → mega-unit** (Lane's idea: collapse same-type collectors to save resources). Select 2+ harvesters / coolant tankers / alloy haulers / loggers and press **G** (or tap **⊕ COMBINE COLLECTORS**) → they merge into ONE unit with `Unit.stack` = N that gathers + carries ×N (cargo cap, fill rate, HP all scale ×stack). Economically identical — verified a ×5 mega delivers exactly 5× a single harvester (1050 vs 210) — but **1 entity instead of N** (saves sim/render cost + micro). Renders bigger; selection panel shows **×N**. **Combat units intentionally excluded** (a 20×-HP "mega tank" would break splash/focus-fire tactics). Hotkey + COMBINE button (mobile) + help-panel entry.
- **Mobile UX redesign — command drawer** (Lane: "too much on too small a screen"; phone is for demo/test, desktop is the real play surface). On phones (≤760px) the command sidebar is now a **slide-in drawer, closed by default** → the battlefield gets the **full screen** (canvas 375px) at a **1.7× default zoom** so units/terrain are tappable. A floating **☰ BUILD / ✕ CLOSE** FAB toggles it; picking a building or ability **auto-closes** it so you return to the full map to place/target. Drawer is wider (260px) with bigger, readable buttons + a larger minimap. **Desktop is untouched** (verified: 1280px = normal 288px in-flow sidebar, no toggle). All in the `@media(max-width:760px)` block + a small toggle wired in `sidebar.ts` + responsive zoom in `BattleScene`.
- **Easy long wall sections + Timber Palisade** — placing a Wall/Gate/Palisade now **stays armed** so you lay a whole line with repeated clicks (right‑click / Esc stops); normal buildings still place one‑at‑a‑time. New **Timber Palisade** (`B.palisade`, ▣20 + 🪵40, hp650) — a cheap wood‑cost barrier giving the wood economy a 2nd sink (wood previously only fueled Repair Rigs); `BuildingDef.wood` cost wired through `startPlacing`/`tryPlace` + shown on the build button. Verified headlessly (4 laid in a row, stays armed, correct wood/crystal spend, blocks movement).
- **Controls & legend help panel** — a re-openable **⌨ KEYS** button in the topbar opens a CONTROLS & LEGEND overlay documenting everything (select/order, control groups, camera, pause + game speed, build/sell, walls/gates, society/conscript, cyber abilities, the Free Legion threat, victory). Intro controls list also updated with the new keys (Space / `[` `]` / Del) + the Free Legion warning. Closes the discoverability gap from all the new systems.
- **Full emergent faction — the FREE LEGION (team 7)** — after **3 militia uprisings**, the movement *coalesces into a real 7th faction*: it founds a stronghold at the uprising town (HQ + power + refinery + harvester), all risen militia rally to it, it gets a founding warband, a full economy + the standard AI (it builds, trains, expands, attacks), diplomacy (war with the player + the faction whose land it rose in, tense with the rest), and it's in the win condition (you must destroy it to win). Required making the team roster **dynamic**: `ALL_TEAMS`/`AIS` are now mutable + reset each match; `addAITeam`/`resetTeams`; `EMERGENT_TEAM`=7; `FAC[7]` (magenta); per‑team state spun up by `initFactionState`; `BASE_INFO[7]` set at founding; a `setEmergeHook` so the renderer bakes team‑7 sprites the frame it appears (`buildTeamTextures`). Verified headlessly: founds correctly, runs 40s stable growing 3→13 buildings / 20→35 units, and a restart cleanly returns to 6 factions. ⚠ feel/balance needs playtest. (Optional next: negotiate-with-the-Legion diplomacy.)
- **Society layer depth (slice 1)** — two ways the civilian layer now bites: **(1) capture windfall** — taking a settlement grants looted crystals (≈pop×8) + 1–3 armed recruits on top of the population, so settlements are worth fighting over; **(2) Free Militia uprisings** — a large, *ungoverned* neutral town (pop ≥26) left neglected for ~75s rises into an armed mob of **team‑0 militia** that's hostile to **every** faction and marches on the nearest base (capped at 32; warns if near the player). New `FAC[0]` (grey, render‑only), `U.militia`, `orderMilitia`/`militiaTick`, rel(0,·)=−100 in setupBases. Verified headlessly (windfall +240cr/+3 recruits; uprising spawns 3 hostile militia that fight & get fought). Full named 7th faction = bigger follow‑up (dynamic teams).
- **FIX: brief freeze when a unit dies** — `destroy()` baked a scorch mark into the terrain canvas + flagged it dirty, so the next frame did a full `terrainTex.refresh()` (whole-texture GPU re-upload) on **every death**. Now scorch marks are cheap **pooled decal sprites** (`addScorch`, capped ring buffer of 160, depth −90) — no terrain re-upload. Remaining terrain re-uploads (forest‑clear / dried water) are coalesced to ≤~1 per 180ms. Verified: scorch texture bakes + decals stamp; clean boot.
- **Real CC0 audio** — real **explosion** (unit/building death) + **nuke blast** + **victory** stinger now play from real public‑domain / CC0 samples (Wikimedia PD explosions + Kenney CC0 jingle), transcoded OGG→mono AAC/**M4A so Safari decodes them**, inlined for the single‑file build (+70KB). New sample‑loader in `audio.ts` (`loadSamples`/`playSample`) decodes on first audio init with **procedural fallback** if a sample isn't ready; nuke layers the sample over a synth sub‑bass. Defeat sting + weapons/UI/EMP/klaxon stay procedural. Verified: all 3 inline as `data:audio/mp4` and decode in‑browser to the right durations. ⚠ sound quality is Lane's ear to judge. Credits in `src/assets/CREDITS.md`.
- **FIX: black map on mobile (iOS)** — the ×3 map baked terrain to a single 10752²≈115MP canvas, which exceeds iOS Safari's ~16.7MP canvas cap → it came back blank/black (UI + units still drew). Now baked at a reduced resolution (`TERRAIN_RES`, ≈3464²=12MP, under the cap) via a persistent scale transform in `terrain.ts` (all drawing stays in world coords; scorch/clear-forest/dry-water hooks included), and the texture is upscaled to world size on display (LINEAR filter). Bonus: ~10× less terrain GPU memory + faster upload on every device. Verified canvas ≤ iOS limits + still renders correctly on desktop; Lane to confirm on iPhone.
- **AI parity — defensive + Hijack**: the AI now builds a **Water Tower**, fortifies its front with **Walls + a Blast Gate** (sparse so it never walls itself in — verified its units can still path out), and casts **System Hijack** to steal an enemy combat unit (pays the cost). With EMP (already shipped) the AI now uses the full cyber kit. Headless‑verified.
- **Blast Gates (team‑aware)** — a doorway in your wall line: your units + allies path through freely, enemies are blocked and route around (or smash it). Done properly via team‑aware pathfinding: new `passableFor(tx,ty,team)` + per‑tile `game.gate` owner grid; `findPath`, `unitBlocked`, movement & separation all thread the unit's team. An enemy gate reads as a wall to you. Verified headlessly (owner path 0‑tile deviation through the gate; enemy detours 7 tiles around).
- **Fortified Walls** — new cheap, tough 1×1 barrier building (`B.wall`, 70cr / 1200hp / no power). Impassable (units path around it automatically — buildings already block via `game.occupied`), sellable, crenellated sprite + sidebar icon. Funnel attackers into your turrets. (Gates + wood‑palisades remain — see Up next.)
- **Pause + game speed** — Space = pause/resume; `[` / `]` (or `-` / `+`) cycle 1×→2×→3×. Implemented as fixed sub‑steps per frame (stable movement/pathing, not one big dt). On‑screen badge + feed messages; resets on new match.
- **Inbound‑missile klaxon** — distinct air‑raid siren SFX (`sfx('klaxon')`) replaces the generic 'war' beep when a missile is inbound at the player. (Full CC0 sample pack still in Up next.)
- **AI parity Phase 2 — AI uses its whole toolbox**: `aiTech` now also builds **Lumber Mill** (→ Loggers + **Repair Rigs** for map‑wide field repair), **Cyber Ops Center**, and **Deep Bore Facility**; the AI trains **Repair Rigs**, **Survey Hunters**, and **Borers**; **hero parity** — AI Hunters survey vaults (per‑team `discBy`, no player fog leak) and idle Borers excavate them, so the AI fields heroes too; and the AI fires **EMP** from a Cyber Ops Center at enemy clusters (pays the cost). Verified headlessly (builds + trains + hero excavation→Titan + EMP all pass). ⚠ feel needs playtest. Leftovers: AI Water Tower + AI Hijack.
- **Performance pass 2 — smoothness**: throttled the **strategic AI loop** to ~3Hz (it re‑scanned every unit per faction every frame) and **player fog recompute** to ~15Hz, both fed accumulated dt so rates stay correct. Combat/targeting/movement stay per‑frame. (Web Worker sim deferred as the next lever if needed.)
- **Performance — Safari lag on the 3× maps**: the per‑frame **fog** and **minimap** passes (each touching all 336²=112,896 tiles every frame) were the bottleneck. Now: both throttled to ~14Hz (vision changes a few×/s, not 60×); fog reuses a persistent buffer with alpha‑only writes (no per‑frame `getImageData` allocation); minimap terrain is a cached ImageData blit instead of up to 112k `fillRect` calls/frame; overlay is viewport‑culled with cached faction colours. ⚠ FPS unmeasurable headlessly (Phaser pauses RAF when backgrounded) — Lane to confirm on device.
- **AI parity Phase 1 — missile/defense counterplay**: the AI now conditionally builds **Iron Domes** + **Missile Silos** (new `aiTech` pass, robust to timing — builds each once as money/alloy allow; warlords reach for the Silo first, others shield up first). AI missile launches are now **gated on owning a Silo**, cost money/alloy, respect a per‑AI cooldown, and escalate to **thermonuclear** when flush — so the player's nuke is no longer an auto‑win and the player's own Iron Dome earns its keep on defense. Verified headlessly (intercept + control + AI‑builds + AI‑launches all pass); ⚠ feel needs Lane's playtest.

## ✅ Shipped in Session 2 (newest first) — detail in `docs/SESSION_3_HANDOFF.md`
- Unit **veterancy** (kills → Veteran/Elite, +dmg/HP, chevrons).
- Survey Hunter **auto‑hunt**; **turret/flak range** buffs.
- **Missile & defense system**: Missile Silo, Thermonuclear Missile (faction‑killer), Iron Dome (building) + Aegis (mobile) interceptors, AI missile launches, inbound warnings.
- **Terrain** improvement (CC0 textures + macro light/shadow); real **CC0 trees**.
- **Underground‑targeting fix**; **water harvesting** + Water Tower + coolant buff.
- **Hero system** (vaults, Survey Hunter, Borer excavation, 3 heroes).
- **Subterranean Borer** + Deep Bore Facility; **harvester tunneling**.
- **Wood logistics**: Logger + Lumber Mill + Repair Rigs (map‑wide auto‑repair).
- **FX/atmosphere polish**: embers, tracers, vignette, motes; topbar chips + mobile topbar fix.
